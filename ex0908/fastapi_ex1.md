
## Fastapi 설치
```bash
pip install fastapi[standard]
```


## Fastapi 실행

``` bash
uvicorn main:app --reload
```
or
``` bash
fastapi dev main.py
```

- --reload : 수정한 부분 자동 반영
- 127.0.0.1 == localhost

## Django / Flask
- python 전용 api